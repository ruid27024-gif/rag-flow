
  return (

    
    // 侧边栏
    // 添加了 bg-gray-100，这是一个非常舒适的浅灰色背景
    <section className="pt-1 px-4 w-[296px] flex flex-col bg-gray-50 ">
      {/* <PageHeader>
          <Breadcrumb>
            <BreadcrumbList className="!items-start">
              <BreadcrumbItem>
                <BreadcrumbLink
                  onClick={navigateToChatList}
                  className="group relative inline-flex items-center"
                >
                  <span className="text-2xl font-bold text-black">
                    {t('chat.chat')}
                  </span>

                  <span className="absolute left-0 top-full mt-3 w-max max-w-xs px-3 py-1.5 text-xs font-medium text-gray-800 bg-black/10 backdrop-blur-sm border border-gray-200/50 rounded-lg shadow-lg opacity-0 translate-y-2 group-hover:opacity-100 group-hover:translate-y-0 transition-all duration-300 pointer-events-none z-50">
                    点击返回聊天页面
                    <span className="absolute -top-[7px] left-6 w-3 h-3 bg-transparent border-l border-t border-gray-300/60 rotate-45"></span>
                  </span>

                </BreadcrumbLink>
              </BreadcrumbItem>
              <Button onClick={showEmbedModal}>
                <Send />
                {t('common.embedIntoSite')}
              </Button>
            </BreadcrumbList>
          </Breadcrumb>
      </PageHeader> */}

      <PageHeader >
      <Breadcrumb>
        <BreadcrumbList className="!items-start">
          <BreadcrumbItem>
            <BreadcrumbLink
              onClick={navigateToChatList}
              className="group relative inline-flex items-center"
            >
              {/* 绝对能生效的文字部分 */}
              <span 
                className="text-2xl font-bold transition-all duration-300 inline-block hover:-translate-y-0.5"
                style={{
                  color: '#000', // 默认纯黑色
                  WebkitTextFillColor: '#000', // 兜底：强制默认文字为黑色
                  WebkitBackgroundClip: 'text',
                  backgroundClip: 'text',
                  backgroundImage: 'linear-gradient(to left, #22d3ee, #34d399)'
                }}
                onMouseEnter={(e) => {
                  // 悬浮时：文字变透明，透出底下的渐变
                  e.currentTarget.style.WebkitTextFillColor = 'transparent';
                  e.currentTarget.style.color = 'transparent';
                }}
                onMouseLeave={(e) => {
                  // 移开时：恢复纯黑色
                  e.currentTarget.style.WebkitTextFillColor = '#000';
                  e.currentTarget.style.color = '#000';
                }}
              >
                {t('chat.chat')}
              </span>

              {/* 悬浮提示框（保持原样） */}
              <span className="absolute left-0 top-full mt-3 w-max max-w-xs px-3 py-1.5 text-xs font-medium text-gray-800 bg-black/10 backdrop-blur-sm border border-gray-200/50 rounded-lg shadow-lg opacity-0 translate-y-2 group-hover:opacity-100 group-hover:translate-y-0 transition-all duration-300 pointer-events-none z-50">
                点击返回导航页面
                <span className="absolute -top-[7px] left-6 w-3 h-3 bg-transparent border-l border-t border-gray-300/60 rotate-45"></span>
              </span>
            </BreadcrumbLink>
          </BreadcrumbItem>
        </BreadcrumbList>
      </Breadcrumb>
    </PageHeader>



      {/* <section className="flex items-center text-base justify-between gap-2"> */}
      <div className="flex items-center justify-between mt-6 pr-5">
        <div className="flex gap-3 items-center min-w-0">
          <RAGFlowAvatar
            avatar={data.icon}
            name={data.name}
            className="size-8"
          ></RAGFlowAvatar>
          <span className="flex-1 truncate">{data.name}</span>
        </div>
        <PanelLeftClose
          className="cursor-pointer size-4"
          onClick={switchVisible}
        />
      </div>
      
      {/* </section> */}


      {/* <div className="flex justify-between items-center mb-4 pt-10">
        <div className="flex items-center gap-3">
          <span className="text-base font-bold">{t('chat.conversations')}</span>
          <span className="text-text-secondary text-xs">
            {conversationList.length}
          </span>
        </div>
        <Button variant={'ghost'} onClick={addTemporaryConversation}>
          <Plus></Plus>
        </Button>
      </div> */}

      <div className="flex flex-col gap-2 mb-0 pt-3 w-full"> 
        <div
          ref={(el) => {
            if (el) {
              el.onmouseenter = () => {
                el.style.backgroundColor = '#eff6ff';
                el.style.borderColor = '#93c5fd';
                const icon = el.querySelector('svg');
                const text = el.querySelector('span');
                if (icon) icon.style.color = '#2563eb';
                if (text) text.style.color = '#2563eb';
              };
              el.onmouseleave = () => {
                el.style.backgroundColor = '#f9fafb';
                el.style.borderColor = '#e5e7eb';
                const icon = el.querySelector('svg');
                const text = el.querySelector('span');
                if (icon) icon.style.color = '#6b7280';
                if (text) text.style.color = '#374151';
              };
            }
          }}
          onClick={addTemporaryConversation}
          className="flex items-center justify-center gap-2 w-full h-[60px] px-4 rounded-md cursor-pointer
            border border-gray-200 bg-gray-100
            transition-all duration-300 ease-in-out
            shadow-sm hover:shadow-md"
          style={{ height: '35px' }} // 内联样式确保高度
        >
          {/* 这里是用 SVG 画的“新建对话”图标 */}
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            width="16" 
            height="16" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            strokeWidth="2" 
            strokeLinecap="round" 
            strokeLinejoin="round"
            className="text-gray-500 group-hover:text-blue-600 transition-colors duration-300"
          >
            {/* 对话气泡的外框 */}
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            {/* 里面的加号 */}
            {/* <line x1="12" y1="11" x2="12" y2="17"></line>
            <line x1="9" y1="14" x2="15" y2="14"></line> */}
          </svg>

          <span className="text-sm font-medium text-gray-700 group-hover:text-blue-600 transition-colors duration-300">
            新建对话
          </span>
        </div>
      </div>



      <div className="flex flex-col gap-2 mb-1 pt-3 w-full">
        {/* <Button
          onClick={addTemporaryConversation}
          variant="transparent"
          className="group flex items-center justify-start gap-3 h-[52px] px-3 w-full 
            border border-gray-200 
            bg-gray-100 text-blue-600 
            transition-all duration-300 shadow-sm"
        >
          <Plus className="w-4 h-4 text-black transition-colors duration-300" />
          <span className="text-sm font-medium text-black transition-colors duration-300">新建对话</span>
        </Button> */}

        <div className="pb-1">
        <SearchInput
          onChange={handleInputChange}
          value={searchString}
          placeholder="搜索历史对话"
        ></SearchInput>
        </div>
      </div>

{/* 标题与数量统计：极简大气风 */}
{/* 标题与数量统计：经典胶囊风（缩小了上方间距） */}
<div className="flex items-center justify-between mb-2 mt-4 px-1 w-full">
  <span className="text-small font-medium text-gray-800 tracking-tight">
    {t('chat.conversations')}
  </span>
  <span className="text-xs font-medium text-blue-600 bg-blue-50/80 px-2 py-0.5 rounded-full border border-blue-100/50">
    {conversationList.length}
  </span>
</div>
    
      


      <div className="space-y-2 flex-1 overflow-auto">
        {conversationList.map((x) => (
          <Card
            key={x.id}
            onClick={
                      handleCardClick(x.id, x.is_new
                      )}
                        className={
                      cn
                      (
                          'cursor-pointer bg-transparent !border-none !shadow-none'
                      ,
                          {
                            'bg-bg-card': conversationId === x.id
                      ,
                            'hover:bg-gray-100': conversationId !== x.id, // 非选中状态悬浮灰色
                            'hover:bg-gray-130': conversationId === x.id, // 选中状态悬浮深一点
                          }
                        )}
          >
            <CardContent className="px-3 py-2 flex justify-between items-center group gap-1">
              <div className="truncate">{x.name}</div>
              <ConversationDropdown
                conversation={x}
                removeTemporaryConversation={removeTemporaryConversation}
              >
                <MoreButton></MoreButton>
              </ConversationDropdown>
            </CardContent>
          </Card>
        ))}
      </div>

      <div className="py-2">
        <Button
          className="w-full"
          onClick={switchSettingVisible}
          disabled={!hasSingleChatBox}
          variant={'outline'}
        >
          {t('chat.chatSetting')}
        </Button>
      </div>
    </section>
  );
}