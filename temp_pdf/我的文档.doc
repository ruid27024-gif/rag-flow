重新安装当然可以，不过既然问题出在文件本身是个“空壳”上，直接重装可能解决不了根本问题。建议你先试试下面这个更直接的办法，如果不行再考虑重装。
方法一：先别重装，试试这招（最快）
刚才的问题是因为那个文件是空的（0字节），软件不知道该怎么处理它。我们换个思路，不要创建空文件，而是直接让软件生成一个新文件。
在终端里输入这一行命令：
bash
libreoffice --writer
（注意：后面没有文件名）
效果：这会直接强制启动 LibreOffice 的文字处理界面（Writer）。等界面出来后，你再在里面打字，然后点“保存”，这样生成的文件绝对是正常的。
方法二：如果一定要重装（最彻底）
如果你还是想重装来修复可能存在的软件问题，请按照以下步骤操作，这能把旧版本清理干净：
    1. 卸载旧版本
在终端输入：
       bash
       sudo apt purge libreoffice*
       （输入密码时不会显示字符，输完回车即可）
    2. 清理残留
接着输入：
       bash
       sudo apt autoremove
    3. 重新安装
最后输入：
       bash
       sudo apt install libreoffice
装好后，重新打开软件，点击“Writer Document”应该就能正常使用了。

